import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            main: "#03a9f4"
        },
        secondary: {
            main: "#e91e63"
        }
    },
    background: {
        default: "#fff"
    }
})

export default theme;